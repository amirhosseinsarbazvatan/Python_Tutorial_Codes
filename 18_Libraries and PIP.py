# PIP in python

# Modules and libraries in Python:
# Standard libraries
# External library
# Manual library

# Standard Libraries
# Like math, os, sys, statistic, random ,...

import math
print(math.sqrt(4))


from math import sqrt
print(sqrt(4))

from math import sqrt as sq
print(sq(4))

from math import *
print(sqrt(4))


# External library
# Install by pip
# Download and install pip => Use CMD or terminal for write command

# pip                         => Display pip informations
# pip help                    => Pip giudness
# pip list                    => List of installed package
# pip freeze                  => List of installed package and versions
# pip install packagename     => Install package
# pip unistall packagename    => Unistall package